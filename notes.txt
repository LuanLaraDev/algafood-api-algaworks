O desafio da aula é tratar exceções que não são capturadas.

Pra isso criamos um ProblemType para representar um erro no sistema. Depois criamos um método na classe
ApiExceptionHandler que será responsável por receber uma exceção do tipo Exception.