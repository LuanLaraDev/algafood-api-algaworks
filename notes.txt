Podemos também, ao invés de usar query methods, utilizar a anotação @Query e definir a query que será realizada pelo
método.

Desta forma, podemos melhorar a legibilidade dos métodos quando necessário e tembém realizar consultas mais complexas.