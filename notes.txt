Podemos também ter a necessidade de alterar um recurso parcialmente, fazemos isso por meio do método HTTP Patch

Iremos criar uma implementação para o resource de Restaurante.

Primeiramente iremos criar o método "atualizarParcial", que irá receber o restauranteId e um Map com os valores
passados no corpo da requisição.

Na próxima aula prosseguiremos com a criação do método, onde utilizaremos da Reflection API para atualizar os valores
do resource.